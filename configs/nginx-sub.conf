# Nginx 反代配置 — 将 /sub/ 路径转发到 hy2-gateway 管理 API
#
# 用法：将此文件 include 到你现有的 server block 中，或者把内容复制过去。
#   server {
#       listen 443 ssl;
#       server_name your.domain.com;
#       ...
#       include /path/to/nginx-sub.conf;
#   }

# 订阅接口 — 无需鉴权，token 本身就是凭证
location /sub/ {
    proxy_pass http://127.0.0.1:9090;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    # 禁止缓存，确保每次拉取最新配置
    proxy_no_cache 1;
    proxy_cache_bypass 1;
    add_header Cache-Control "no-store, no-cache, must-revalidate";
}

# （可选）管理接口 — 仅允许本地或指定 IP 访问
# 如果你也想通过 Nginx 暴露管理 API，取消下面的注释
# location /traffic {
#     allow 127.0.0.1;
#     allow YOUR_ADMIN_IP;
#     deny all;
#
#     proxy_pass http://127.0.0.1:9090;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
# }
